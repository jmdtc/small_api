export default {
  page: ["page", "per_page"],
  interval: ["power_trust", "ref_domains", "price"],
  float_interval: ["visibility"],
  string: ["domain_name", "lang", "country"],
  boolean: ["blacklist", "agency"],
  array: ["category"]
}
